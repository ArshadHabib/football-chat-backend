# name: Deploy Football Chat Backend

# on:
#   push:
#     branches:
#       - master

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Deploy to Server via SSH
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.SERVER_HOST }}
#           username: ${{ secrets.SERVER_USERNAME }}
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           port: 22
#           script: |
#             set -e  # Exit immediately if any command fails
#             cd /var/www/football-chat-backend
#             echo "Starting deployment..."
#             git reset --hard
#             git pull

#             if git diff --name-only HEAD~1 HEAD | grep -q package.json; then
#               echo "package.json changed - running npm install..."
#               npm install
#             else
#               echo "No changes in package.json - skipping npm install"
#             fi

#             # Check if changes are ONLY in the 'files' directory
#             if git diff --name-only HEAD~1 HEAD | grep -qv '^files/'; then
#               echo "Changes detected outside 'files' folder - restarting server..."
#               pm2 restart 0
#             else
#               echo "Only changes in 'files' folder - skipping server restart"
#             fi

#             echo "Deployment completed successfully!"
